# OIRIA watch OS — ESP-IDF *project* (ESP32-S3).
# Clone this repo and run `idf.py build` from HERE.
# Do not nest this tree inside examples/tusb_hid/main/.
cmake_minimum_required(VERSION 3.16)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(oiria_os)
